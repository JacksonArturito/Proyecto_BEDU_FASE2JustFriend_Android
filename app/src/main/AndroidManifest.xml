<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.justfriend">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JustFriend">
        <activity android:name=".DetailSudaderaActivity"></activity>
        <activity android:name=".DetailPlayeraActivity" />
        <activity android:name=".JuegoActivity" />
        <activity android:name=".StickerActivity" />
        <activity android:name=".SudaderaActivity" />
        <activity android:name=".PlayerasActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>